apiVersion: v1
kind: ConfigMap

metadata:
  name: notification-service-config
  namespace: {{ .Values.global.namespace }}

data:
  APP_NAME: {{ .Values.notification.config.APP_NAME | quote }}
  PORT: {{ .Values.notification.config.PORT | quote }}
  ENVIRONMENT: {{ .Values.notification.config.ENVIRONMENT | quote }}
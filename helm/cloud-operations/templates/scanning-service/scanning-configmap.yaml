apiVersion: v1
kind: ConfigMap

metadata:
  name: scanning-service-config
  namespace: {{ .Values.global.namespace }}

data:
  APP_NAME: {{ .Values.scanning.config.APP_NAME | quote }}
  PORT: {{ .Values.scanning.config.PORT | quote }}
  ENVIRONMENT: {{ .Values.scanning.config.ENVIRONMENT | quote }}